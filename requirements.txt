fastapi
uvicorn[standard]
httpx
requests
python-dotenv
gTTS
# Opcional — só se quiser usar Claude (Anthropic) em vez do modelo local.
# ATENÇÃO: Claude TEM filtro. Para o modo sem filtro, deixe ANTHROPIC_API_KEY vazio no .env.
anthropic
