# Segredos e config local — NUNCA versionar
.env
*.env
secrets*

# Dados locais / sessões / bancos
*.db
*.sqlite
reports/
audio/
__pycache__/
*.pyc

# Modelos (baixados via `ollama pull`, não no git)
*.gguf
*.bin
